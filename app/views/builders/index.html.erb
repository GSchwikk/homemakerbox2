<p id="notice"><%= notice %></p>

<h1>Listing Builders</h1>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @builders.each do |builder| %>
      <tr>
        <td><%= builder.name %></td>
        <td><%= builder.address %></td>
        <td><%= builder.phone %></td>
        <td><%= builder.website %></td>
        <td><%= link_to 'Show', builder , class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_builder_path(builder) , class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', builder, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Builder', new_builder_path %>
